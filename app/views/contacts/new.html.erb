<h1>Contact Form </h1>
<%= simple_form_for @contact do |f| %>
  <%= f.input :name %>
  <%= f.input :email %>
  <%= f.input :message %>
  <%= f.input :address %>
  <%= f.button :submit, class: "btn btn-primary", value: "Submit"%>
<% end %>
